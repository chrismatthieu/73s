<%=
if me
  l = { :caption => 'Edit', :url => edit_profile_path(@p), :html_options => {} }
else
  l = {  }
end
  r = { :back => true, :caption => 'Back', :url => "/", :html_options => {} }
  te_navigation_bar( r, "73s.org", l )
%>

<% if params[:public_view].blank? && @p && @p == @profile %>

<%= render :partial => "profiles/private" %>


<% else %>


<%= render :partial => "profiles/public" %>


<% end %>